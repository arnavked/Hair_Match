/**
 * Camera Barrel Export — HairMatch
 */

export {
  startCamera,
  captureFrame,
  stopCamera,
  isCameraSupported,
} from './camera-capture';
export type { CameraConfig } from './camera-capture';
